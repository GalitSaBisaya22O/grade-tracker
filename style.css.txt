:root {
    --aims-blue: #1a365d;
    --aims-light: #2c5282;
    --accent: #4299e1;
    --danger: #e53e3e;
    --success: #38a169;
    --bg: #f7fafc;
}

body { font-family: 'Segoe UI', sans-serif; margin: 0; background: var(--bg); color: #2d3748; }
.hidden { display: none !important; }

/* Login */
.overlay {
    position: fixed; inset: 0; background: linear-gradient(135deg, var(--aims-blue), #000);
    display: flex; justify-content: center; align-items: center; z-index: 1000;
}
.login-box { background: white; padding: 40px; border-radius: 12px; width: 300px; text-align: center; }
.logo { font-size: 2.5rem; font-weight: 900; color: var(--aims-blue); margin-bottom: 10px; }
.login-box input { width: 100%; padding: 12px; margin: 8px 0; border: 1px solid #ddd; border-radius: 6px; box-sizing: border-box; }
.login-box button { width: 100%; padding: 12px; background: var(--aims-blue); color: white; border: none; border-radius: 6px; font-weight: bold; cursor: pointer; }

/* Layout */
.sidebar { width: 240px; height: 100vh; background: var(--aims-blue); color: white; position: fixed; }
.profile { padding: 30px 20px; text-align: center; border-bottom: 1px solid #2d3748; }
.avatar { width: 50px; height: 50px; background: var(--accent); border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 10px; font-weight: bold; }
.sidebar ul { list-style: none; padding: 0; }
.sidebar li { padding: 15px 25px; cursor: pointer; transition: 0.2s; }
.sidebar li:hover { background: var(--aims-light); }
.logout-btn { color: #feb2b2; margin-top: 40px; }

.content { margin-left: 240px; padding: 40px; }
header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; border-bottom: 2px solid #e2e8f0; padding-bottom: 10px; }
#searchBar { padding: 10px; width: 250px; border: 1px solid #cbd5e0; border-radius: 6px; }

/* Components */
.card { background: white; padding: 25px; border-radius: 10px; box-shadow: 0 4px 6px -1px rgba(0,0,0,0.1); margin-bottom: 20px; }
.grid { display: grid; grid-template-columns: 2fr 1fr; gap: 20px; }
.form-group { display: flex; gap: 10px; }
.form-group input { padding: 12px; border: 1px solid #ddd; border-radius: 6px; flex: 1; }

/* Table */
table { width: 100%; border-collapse: collapse; }
th { text-align: left; background: #edf2f7; padding: 15px; font-size: 0.8rem; text-transform: uppercase; }
td { padding: 15px; border-bottom: 1px solid #edf2f7; }

/* Dynamic Colors */
.clickable { background: #fffaf0; cursor: pointer; text-align: center; font-weight: bold; border: 1px dashed #cbd5e0 !important; }
.clickable:hover { background: #ebf8ff; border-color: var(--accent) !important; }
.btn-primary { background: var(--aims-blue); color: white; border: none; padding: 12px 20px; border-radius: 6px; cursor: pointer; }
.btn-danger { background: var(--danger); color: white; border: none; padding: 8px 15px; border-radius: 4px; cursor: pointer; }

.pass { color: var(--success); font-weight: bold; }
.fail { color: var(--danger); font-weight: bold; }
.crit { color: white; background: var(--danger); padding: 2px 8px; border-radius: 4px; font-size: 0.8rem; }
.safe { color: white; background: var(--success); padding: 2px 8px; border-radius: 4px; font-size: 0.8rem; }
